# airesilient.org

Starter content for a GitHub Pages project site using the built-in `jekyll-theme-minimal`.

After enabling Pages and pushing to `main`, the site URL will be:

`https://eglassman.github.io/airesilient.org/`

TODO: point airesilient.org at this location too
TODO: add citation information to _config.yml